apiVersion: v2
name: sigx-actors-test
description: >-
  Multi-silo @sigx/actors cluster for the AKS scale-out/perf test: N silo
  pods, an in-cluster Redis (membership/directory/actor state), and an
  optional load-generator Job — all from one image, everything values-driven.
type: application
version: 0.1.0
appVersion: "0.1.0"
